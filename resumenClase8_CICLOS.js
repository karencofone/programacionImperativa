// ! ESTRUCTURA FOR

// * for (inicio; condición ; modificador) {
// *    código que se ejecutará en cada repetición
// * }

/*
for (let vuelta = 1; vuelta <= 5; vuelta++) {
    console.log('Dando la vuelta número ' + vuelta);
};
*/   
// ! ESTRUCTURA WHILE

// * while (condicion) {
// *    código que se ejecutará en cada repetición
// *    hace algo para que la condición eventualmente se deje de cumplir
// * }

/*
let vuelta = 1
while (vuelta <= 5) {
  console.log('Dando la vuelta número ' + vuelta);
  vuelta++ //al final de cada vuelta sumara 1 a vuelta
};
*/

// ! ESTRUCTURA DO WHILE
// * El ciclo do while es similar al while, pero se diferencia en que, sin importar la condición, la acción
// * se realizará al menos una vez. NO SE VERIFICA LA PRIMERA VEZ, EN WHILE SE VERIFICA SIEMPRE.

// * let vuelta = 5
// * do{
// *   console.log('Dando la vuelta número ' + vuelta);
// *   vuelta++ // --- Se suma 1 a vuelta por lo tanto vuelta = 6
// * } while(vuelta <= 5); // --- al vuelta ser 6 la condición retorna false y se termina el bloque de código

    